# Claude Prompt — Workbook v11 Enhancement

Paste this into Claude (fresh session or this one) to fix the confirmed problems in Literature
Workbook v11, grounded in the strict independent audit already on file. Status: prompt only, not
yet executed.

## Prompt

```text
Fix Literature Workbook v11 (the file "VEGO-AI_Literature_Workbook_RQ_Only_Organized_v11_GitHub_
Aligned.xlsx", supplied by Ali from Downloads, not currently tracked in this git repo - 4 sheets:
RQ, RQ1, RQ2, RQ3) against the findings in an independent, adversarially-verified audit already on
file at docs/research/phd-proposal/literature-review-v16-workbook-v11-verification-report.md. Read
that report in full before doing anything else - it is the source of truth for what is broken and
why, including exact sheet/row/cell locations for every finding. Also read
docs/agent-memory/issues.md entries ISS-036 and ISS-038, which track the two problem clusters most
relevant to this workbook.

Before touching content: confirm you are editing the actual .xlsx file directly (this workbook
appears to be a hand-maintained spreadsheet, not something generated by a script in this repo - a
search turned up no build script for it), and preserve its existing structure, formulas, and
formatting rather than regenerating it from scratch, since regenerating risks losing conventions
(the priority rubric, the evidence-class columns, the gap tables) that are not fully described
anywhere outside the file itself. If you find a generation script does exist that you were not
told about, use it instead of hand-editing.

Once you've confirmed the real editing target, work through these confirmed problems. Gap G6 - one
of the paired PDF's own six claimed residual gaps - does not appear anywhere in any of the four
sheets' gap tables (workbook_RQ's "Gap -> research response -> empirical closure" table runs only
G1-G5; workbook_RQ2, the sheet that per the PDF's Table 8 should own G2+G3+G6, lists only G2 and
G3). Add G6 to whichever sheet(s) the PDF assigns it to, using the PDF's own G6 definition and
falsifier language so the two artifacts describe the same gap the same way - or, if you determine
G6 should not exist as a workbook-tracked gap, say so explicitly in the sheet rather than leaving a
silent gap in the numbering. FT-A/FT-B evidence-maturity labels are inverted between this workbook
and the paired PDF's own Appendix B for at least 6 shared anchor sources across the four sheets:
Bansal et al. 2021 (workbook says FT-A, PDF Appendix B says FT-B), and Kulesza et al. 2015, Aamodt &
Plaza 1994 (appears in both RQ2 and RQ3), Hu et al. 2019, and Schünemann et al. 2017 (workbook says
FT-B for all four, PDF Appendix B says FT-A). Re-derive the correct label for each of these five
distinct papers from their actual source/access maturity (the PDF's own FT-A/FT-B definition is
"source/access maturity for bounded use" - apply that definition freshly rather than just copying
whichever label happens to be in the PDF, in case the PDF's label is the one that's actually wrong),
and make sure every sheet that cites a given paper uses the same, correct label - Aamodt & Plaza in
particular needs to match across RQ2 and RQ3, not just against the PDF. Raykar et al. (2010),
explicitly named a core anchor source for RQ2 in this workbook, does not appear anywhere in the
paired PDF - either add a properly-cited entry for it to the PDF's own Appendix B and References
(coordinate with whoever runs the paired PDF-enhancement prompt, since that is their file to edit,
not yours), or if it should not actually be a core anchor, replace it in this workbook's ranked
anchor list with a source that is genuinely resolvable against the PDF, following the same
"Anchor-set revision" convention already used elsewhere in these sheets (e.g. row 10's note about
Zou et al. and the VEGO-AI manuscript).

Row EXP-008 in workbook_RQ1 states a rate derived from 33 and 26 that does not match basic
arithmetic (33 / 26 = 1.269, not the stated 1.35) - this exact error is inherited verbatim from
docs/research/phd-proposal/chapter-5-preliminary-results.md; fix it in this workbook and flag the
same error in that source file so it does not keep propagating. Rows RES-2 (workbook_RQ2) and RES-3
(workbook_RQ3) cite docs/research/phd-proposal/2026-08-19-chapter4-decisions-packet.md by GitHub URL
for facts that file does not actually state - the real source for both facts is
docs/research/phd-proposal/chapter-4-research-methodology.md and the 2026-08-18 entry in
docs/agent-memory/decisions.md (an unnamed independent implementer for Study 2 and unnamed raters
for Study 3); update both citations to point at the file(s) that actually say what's claimed. The
per-source "priority score" column in all four sheets forms a smooth, strictly monotonic sequence
that exactly tracks rank order with no visible sub-scores, even though the stated rubric (row 12)
claims five weighted sub-criteria (35%/25%/20%/15%/5%) - either show the actual sub-scores that sum
to each priority number so the weighting is checkable, or relabel the column plainly as an editorial
ranking rather than a computed score, matching the honesty of the rubric's own existing caveat
("scores are editorial priorities, not empirical quality ratings").

The "Current exact provisional RQ" row (row 4-6 in all four sheets) already correctly distinguishes
current wording from the demoted v15 candidate wording and cites three-study-contract.md-consistent
language - do not weaken or remove that distinction; if a paired PDF-enhancement pass changes the
PDF's RQ wording to match the canonical text, double check this workbook's own current-wording row
still agrees with whatever the corrected canonical wording turns out to be at that time, since
three-study-contract.md itself could have moved on again by the time you finish.

Two things you should NOT do while fixing the above: do not touch the PDF (v16) as part of this
task - a separate, paired prompt handles that, and fixing this workbook in a way that assumes a PDF
fix has already happened (e.g. assuming Raykar was added to the PDF, or assuming the PDF's RQ
wording has already been corrected) risks being wrong if that other prompt hasn't run yet or made a
different call. State your assumptions about the PDF's current state explicitly in your final
summary so a reader can tell whether this workbook and the PDF are actually in sync yet. Also do not
invent a "no global readiness score is asserted" style disclaimer change without checking what the
PDF's own scorecard section currently says - if the PDF-enhancement prompt has already replaced or
reworked the "76/100" scorecard by the time you run, update this workbook's row 3 language to match
the new reality rather than leaving a stale contradiction in the other direction.

Before declaring this done: re-verify your own fixes with the same rigor the original audit used -
re-check that G6 is now present and consistently defined wherever it should be, re-check the
FT-A/FT-B labels you changed against the actual source/access maturity of each paper rather than
just trusting one side's prior label, re-run the EXP-008 arithmetic, and re-open the two corrected
GitHub-file citations yourself to confirm they now say what's claimed. Update
docs/research/phd-proposal/literature-review-v16-workbook-v11-verification-report.md with a short
"resolved since" note per finding you fixed (or a follow-up report if you prefer a clean v12
verification pass), and update docs/agent-memory (session-log.md, revert-log.md, and issues.md to
close or update ISS-036/ISS-038) per this project's CLAUDE.md convention.
```
