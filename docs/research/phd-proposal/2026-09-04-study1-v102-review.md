# Study 1 v1.0.2 technical review

**Branch:** `review/study1-airtravel-v102`

**Claude amendment consumed:** `ff0a61a08e76b72472924066f11d15f8b08f3726` (source commit; carried to this review branch as a docs-only cherry-pick)

**Provider calls:** 0

**Detector application to AirTravel:** 0

**Synthetic generation:** 0

## Verdict

**TECHNICAL NO-GO** for any provider-backed run. The v1.0.2 amendment and staged AirTravel bytes verify offline, but no protected authorization exists, the production observer is not proven on all natural orchestrator routes, and CI remains red on the pre-existing stale release manifest. This branch is a review artifact only; it must not be merged without hash-bound human authorization.

## Amendment and AirTravel pack

The amendment manifest is `text2uml-airtravel/amendment-manifest-v1.0.2.json`. It freezes `setting_id=cd_airtravel` separately from `corpus_id=text2uml_airtravel_253b26dc`, upstream Text2UML commit `253b26dc704d523209a5cba79686f8f7fab57d63`, N=4, authorship `PUBLIC_EXTERNAL + EXTERNAL_LLM_GENERATED`, and separate historical/synthetic/public-external denominators. The existing non-mutating verifier passes all checks: five runtime files match exactly, references are outside the runtime input path, and provider execution remains disabled. PlantUML is **wrapper/byte validated only**; Java/PlantUML is unavailable locally, so parse/render validation is a blocker rather than being claimed as passed.

## Historical slot audit

The authoritative paper record establishes 178 ordinal slots: `ucd_pw=44`, `cd_pw=41`, `ucd_ch=46`, and `cd_ch=47`. The local ignored inventory contains 165 byte candidates (37, 37, 45, 46), and the supplied archive matches all 165 byte-for-byte. Because no authoritative mapping binds those filenames to the paper’s ordinal slots, the candidates are `RECOVERY_CANDIDATE_UNVERIFIED`, not `ORIGINAL_VERIFIED` or `DERIVED_EXACT_COPY`. All 178 expected slots remain `NOT_YET_SEARCHED` for slot identity purposes; scoped not-found is therefore zero, search closure is false, and synthesis eligibility is zero. No “missing” claim is made.

## Quarantined synthetic branch

The local `study1/synthetic-corpus` worktree contains 180 files under `Dataset1_ModelEval_SYNTHETIC` (178 model files plus two metadata files). The report records each relative path, hash, and byte size as `QUARANTINED_UNADJUDICATED_SYNTHETIC`, with content inspection false. The branch was not merged, renamed, deleted, executed, or used.

## Production instrumentation and gate

The current observer remains insufficiently proven for the requested natural-route standard: five of six route checks are helper fixtures, producer metadata can fall back to Agent-2 context, mixed language/domain questions share a pending list, round suffix normalization is incomplete, and termination is partly fixture-driven. `provider_run_permitted=false` is documentary and is not an enforced pre-construction guard in the protected orchestrator. Therefore no protected runtime files are changed here and no authorization packet is self-issued.

An external gate is provided at `scripts/study1_external_execution_gate.py`. It validates the v1.0.2 pack, exact model, concurrency, call cap, current commit, unexpired hash-bound authorization, and green CI before a client factory can be called. Its negative-path tests prove that a missing condition raises before client construction; the current invocation is blocked as expected.

## Reproducibility artifacts

- `historical-case-recovery-v2/historical-recovery-slot-audit.json`
- `historical-case-recovery-v2/synthetic-branch-quarantine.json`
- `historical-case-recovery-v2/recovery-scope-report.json`
- `scripts/audit_historical_slots.py`
- `scripts/tests/test_audit_historical_slots.py`
- `scripts/study1_external_execution_gate.py`
- `scripts/tests/test_study1_external_execution_gate.py`

The next permitted step is human review of the exact slot identity evidence and a hash-bound authorization for any protected instrumentation change. Even after that, a provider run requires green CI and explicit execution permission.

## Verification snapshot

- Main base: `e999c480eb6536ab71c1f19e54bf914b9ddbd64b`
- Review branch head: `1b959bc44f963e8f08dff3e2905408045b19d005`
- Amendment source commit: `ff0a61a08e76b72472924066f11d15f8b08f3726`
- Local focused tests: slot audit 3 passed; execution gate 4 passed; root 46 passed; VEGO-AI 134 passed.
- Local full scripts suite: 337 passed, 10 skipped, 7 subtests, 1 expected branch-integrity failure because the pilot audit requires `main`.
- Pull-request CI: [run 33884755394](https://github.com/AliHamed17/vego-ai-research/actions/runs/33884755394). Python 3.10/3.11/3.12/3.13 passed; Source/security/documents failed on stale `docs/research/hardening/release-manifest-v3.json`; merge-gate consequently failed.
